---
// Hero section with animated background
---

<section class="relative min-h-screen flex items-center bg-gradient-hero overflow-hidden pt-20">
  <!-- Animated Background Elements -->
  <div class="absolute inset-0 overflow-hidden">
    <!-- Fire particles -->
    <div class="absolute top-1/4 left-1/4 w-64 h-64 bg-terracotta/20 rounded-full blur-3xl animate-pulse-glow"></div>
    <div class="absolute bottom-1/4 right-1/4 w-96 h-96 bg-turquoise/10 rounded-full blur-3xl animate-float"></div>
    <div class="absolute top-1/2 right-1/3 w-48 h-48 bg-terracotta/15 rounded-full blur-2xl animate-float-delayed"></div>

    <!-- Grid pattern -->
    <div class="absolute inset-0 opacity-10">
      <svg class="w-full h-full" xmlns="http://www.w3.org/2000/svg">
        <defs>
          <pattern id="hero-grid" width="40" height="40" patternUnits="userSpaceOnUse">
            <path d="M 40 0 L 0 0 0 40" fill="none" stroke="currentColor" stroke-width="0.5" class="text-turquoise"/>
          </pattern>
        </defs>
        <rect width="100%" height="100%" fill="url(#hero-grid)" />
      </svg>
    </div>
  </div>

  <div class="relative max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-20">
    <div class="grid lg:grid-cols-2 gap-12 items-center">
      <!-- Content -->
      <div class="text-center lg:text-left">
        <div class="inline-flex items-center gap-2 px-4 py-2 bg-turquoise/20 rounded-full text-turquoise text-sm font-medium mb-6">
          <span class="w-2 h-2 bg-turquoise rounded-full animate-pulse"></span>
          Plataforma educativa 100% responsiva
        </div>

        <h1 class="text-4xl sm:text-5xl lg:text-6xl font-black text-white leading-tight mb-6">
          Encendemos el
          <span class="text-gradient-fire"> fuego</span>
          <br />del conocimiento
        </h1>

        <p class="text-lg sm:text-xl text-white/70 mb-8 max-w-xl mx-auto lg:mx-0">
          Como el tlacuache que trajo el fuego a la humanidad, en <strong class="text-turquoise">TLACU</strong> llevamos
          el conocimiento a todos los estudiantes mexicanos.
        </p>

        <div class="flex flex-col sm:flex-row gap-4 justify-center lg:justify-start">
          <a href="#productos" class="px-8 py-4 bg-terracotta text-white font-bold rounded-xl hover:bg-terracotta-light transition-all shadow-xl shadow-terracotta/30 hover:shadow-terracotta/50 hover:-translate-y-1">
            Explorar productos
          </a>
          <a href="#nosotros" class="px-8 py-4 bg-white/10 text-white font-bold rounded-xl hover:bg-white/20 transition-all backdrop-blur-sm border border-white/20">
            Conocer mas
          </a>
        </div>

        <!-- Stats -->
        <div class="grid grid-cols-3 gap-6 mt-12 pt-8 border-t border-white/10">
          <div>
            <div class="text-3xl sm:text-4xl font-black text-white">10K+</div>
            <div class="text-white/50 text-sm">Estudiantes</div>
          </div>
          <div>
            <div class="text-3xl sm:text-4xl font-black text-turquoise">95%</div>
            <div class="text-white/50 text-sm">Satisfaccion</div>
          </div>
          <div>
            <div class="text-3xl sm:text-4xl font-black text-terracotta">1</div>
            <div class="text-white/50 text-sm">Plataforma activa</div>
          </div>
        </div>
      </div>

      <!-- Illustration -->
      <div class="relative flex items-center justify-center">
        <div class="relative">
          <!-- Glow behind mascot -->
          <div class="absolute inset-0 bg-gradient-to-br from-terracotta/40 to-turquoise/40 rounded-full blur-3xl scale-110 animate-pulse-glow"></div>

          <!-- Main logo/mascot -->
          <div class="relative w-72 h-72 sm:w-96 sm:h-96 flex items-center justify-center">
            <div class="absolute inset-0 bg-gradient-to-br from-navy-light/30 to-navy/30 rounded-full"></div>
            <img
              src="/images/logos/isotipo-tlacu-light.webp"
              alt="TLACU Mascot"
              class="relative w-48 h-48 sm:w-64 sm:h-64 object-contain fire-glow animate-float"
              width="256"
              height="256"
            />
            <!-- Decorative ring -->
            <div class="absolute inset-4 border-4 border-dashed border-turquoise/30 rounded-full animate-spin" style="animation-duration: 30s;"></div>
          </div>

          <!-- Floating badges -->
          <div class="absolute -top-4 -right-4 px-4 py-2 bg-white rounded-xl shadow-xl animate-float">
            <span class="text-2xl mr-2">📚</span>
            <span class="font-bold text-navy">ECOEMS</span>
          </div>

          <div class="absolute -bottom-4 -left-4 px-4 py-2 bg-turquoise rounded-xl shadow-xl animate-float-delayed text-white">
            <span class="text-2xl mr-2">🎯</span>
            <span class="font-bold">Proximamente</span>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Scroll indicator -->
  <div class="absolute bottom-8 left-1/2 -translate-x-1/2">
    <a href="#nosotros" class="flex flex-col items-center text-white/50 hover:text-white transition-colors">
      <span class="text-sm mb-2">Descubre mas</span>
      <svg class="w-6 h-6 animate-bounce" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 14l-7 7m0 0l-7-7m7 7V3"></path>
      </svg>
    </a>
  </div>
</section>
